#ifndef PASSWORD_LOADER_H
#define PASSWORD_LOADER_H

#include <vector>
#include <string>

namespace PasswordLoader {
    std::vector<std::string> load_passwords(const char*);
}

#endif /* PASSWORD_LOADER_H */
